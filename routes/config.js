/* just for fileupload to server
 * xying001 2017-09-27
 */
 
 module.exports = { 
     upload: { 
         path: process.cwd() + '/uploads' 
     } 
 } 
//it will create a directory, as: .../uploads/
